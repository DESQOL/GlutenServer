import 'mocha';
import entityTest from './entity';
import helperTest from './helper';
import middlewareTest from './middleware';
import specTest from './spec';

describe('', (): void => {
    describe('Entity', entityTest);
    describe('Helper', helperTest);
    describe('Middleware', middlewareTest);
    describe('Spec', specTest);
});
