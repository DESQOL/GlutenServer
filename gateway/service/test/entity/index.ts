import baseEntity from './baseEntity.test';
import tokenScope from './tokenScope.test';

export default (): void => {
    describe('baseEntity', baseEntity);
    describe('tokenScope', tokenScope);
};
